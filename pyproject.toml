[project]
name = "facecoder"
version = "0.0.1"
description = "A ML project to learn about autoencoders and image-generation"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "kagglehub==1.0.1"
]
